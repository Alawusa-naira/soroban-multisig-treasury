{
  "name": "soroban-multisig-treasury",
  "version": "1.0.0",
  "private": true,
  "description": "Professional Multi-Sig Treasury Suite for the Stellar ecosystem.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "prettier": "^3.3.2"
  },
  "packageManager": "npm@10.8.0"
}
